import 'package:flutter/material.dart';

final Color primaryColor = Colors.blue; // Change to your desired primary color
final Color secondaryColor = Colors.yellow; // Change to your desired secondary color
final Color backgroundColor = Colors.grey[200]; // Change to your desired background color

final ThemeData customTheme = ThemeData(
  primaryColor: primaryColor,
  accentColor: secondaryColor,
  scaffoldBackgroundColor: backgroundColor,
  // Define more theme properties here.
);
